/**
 * Loading UI dla root route
 *
 * Automatycznie wyświetlany przez Next.js podczas ładowania page.tsx.
 * Używa DashboardSkeleton dla spójności UI.
 */

import { DashboardSkeleton } from '@/components/dashboard/DashboardSkeleton'

export default function Loading() {
  return <DashboardSkeleton />
}
